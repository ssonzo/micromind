# Microworld


### Setup
init venv
  * add python

init poetry
  * poetry init
  * add pyproject.toml
  * poetry update

init git
  * git init or git clone
  * add .gitignore
  * add .gitattributes

init git hooks
  * bash tools/copy-git-hooks.sh
